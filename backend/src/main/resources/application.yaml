spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/promochef_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver

pdv:
  datasource:
    url: jdbc:mysql://localhost:3306/pdv_ficticio?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

promochef:
  jwt:
    secret: gJe@I6@@rQz3k&1j7l3ejcBZNqHT!QoQ
    expiration: 28800

server:
  port: 8080